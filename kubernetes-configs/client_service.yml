apiVersion: v1
kind: Service
metadata:
  name: client
  labels:
    name: testing-kube-deploy
    app: testing-kube-app
spec:
  type: NodePort
  selector:
    app: testing-kube-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30001
